version: 0.2

phases:
  install:
    runtime-versions:
      php: 8.1
    commands:
      - echo "Installing dependencies (if any)"

  pre_build:
    commands:
      - echo "Pre-build phase started"

  build:
    commands:
      - echo "Build started"
      - ls -la

  post_build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
