version: 0.2

phases:
  install:
    commands:
      - echo "No runtime needed"

  pre_build:
    commands:
      - echo "Pre-build phase started"

  build:
    commands:
      - echo "Build started"
      - ls -la

  post_build:
    commands:
      - echo "Build completed successfully"

artifacts:
  files:
    - '**/*'
